# Dependências a adicionar no requirements.txt do V5
#
# Verificar se já existem antes de adicionar.
# Instalar com: pip install -r requirements_delta.txt

# ── BTG Fatura XLSX (descriptografia de arquivo protegido por senha)
msoffcrypto-tool>=4.12.0
openpyxl>=3.1.0

# ── Mercado Pago Fatura PDF (OCR — PDF com fonte corrompida)
# ATENÇÃO: easyocr instala PyTorch automaticamente (~600 MB de dependências)
# Na primeira execução os modelos OCR (~100 MB) são baixados para ~/.EasyOCR/
easyocr>=1.7.0

# ── PyMuPDF — renderização de PDF para imagem (usado pelo OCR do MP)
# Provavelmente já instalado. Verificar antes de adicionar.
PyMuPDF>=1.24.0

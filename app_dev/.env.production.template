# ⚙️ Environment Variables for Production Deployment
# Copy this file to .env.production and fill in actual values
# NEVER commit .env.production to git!

# 🔐 Security - CHANGE THESE VALUES!
SECRET_KEY=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING_USE_secrets_token_hex_32
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# 🌐 Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False
FLASK_APP=run.py

# 🗄️ Database Configuration
DATABASE_URI=sqlite:////opt/financial-app/instance/financas.db
# For PostgreSQL (if migrating):
# DATABASE_URI=postgresql://username:password@localhost/financas_db

# 📁 File Paths
UPLOAD_FOLDER=/opt/financial-app/uploads_temp
SESSION_FILE_DIR=/opt/financial-app/flask_session
LOG_FILE=/opt/financial-app/logs/app.log

# 🔒 Security Headers
SECURE_SSL_REDIRECT=True
PREFERRED_URL_SCHEME=https

# 📊 Application Settings
MAX_CONTENT_LENGTH=16777216  # 16MB max file upload
PERMANENT_SESSION_LIFETIME=86400  # 24 hours in seconds

# 🎯 Deployment Info
DEPLOYMENT_ENV=production
VERSION=3.0.1
DEPLOYED_BY=admin
DEPLOYED_AT=2026-01-02

# 📧 Email Configuration (optional - for future notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# 🔔 Monitoring (optional - for future integration)
# SENTRY_DSN=https://your-sentry-dsn
# LOG_LEVEL=INFO

# 🌐 Domain Configuration
# DOMAIN=meuplanner.com.br
# SERVER_NAME=meuplanner.com.br

# 💾 Backup Configuration
BACKUP_DIR=/opt/financial-app/backups
BACKUP_RETENTION_DAYS=30

# 🔄 External Backup (optional - for rsync to remote server)
# REMOTE_BACKUP_HOST=backup-server.example.com
# REMOTE_BACKUP_USER=backup_user
# REMOTE_BACKUP_PATH=/backups/financial-app

# Instructions:
# 1. Copy this file: cp .env.production.template .env.production
# 2. Generate SECRET_KEY: python3 -c 'import secrets; print(secrets.token_hex(32))'
# 3. Fill in actual values
# 4. Set permissions: chmod 600 .env.production
# 5. Never commit .env.production to git!

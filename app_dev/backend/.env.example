# 🔧 Exemplo de Configuração - Backend FinUp
# Copie este arquivo para .env e ajuste os valores

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# DATABASE - Escolha SQLite (dev) ou PostgreSQL (prod)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Opção 1: SQLite (Desenvolvimento local - padrão)
# DATABASE_URL não precisa ser definido, usará o padrão do config.py

# Opção 2: PostgreSQL (Desenvolvimento espelho de produção)
# Descomente e ajuste os valores abaixo:
# DATABASE_URL=postgresql://finup_user:sua_senha_aqui@localhost:5432/finup_db_dev

# Opção 3: PostgreSQL (Produção - VPS)
# DATABASE_URL=postgresql://finup_user:FinUp2026SecurePass@localhost:5432/finup_db

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# AUTHENTICATION - JWT Tokens
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ⚠️ CRÍTICO: Mudar em produção! Mínimo 32 caracteres
JWT_SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# Algoritmo JWT (não modificar)
JWT_ALGORITHM=HS256

# Tempo de expiração do token (em minutos)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# CORS - Origens permitidas
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Desenvolvimento local
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Produção (adicionar domínio)
# BACKEND_CORS_ORIGINS=https://meufinup.com.br,https://www.meufinup.com.br

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SERVER - Configurações do servidor
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Host (0.0.0.0 para aceitar conexões externas)
HOST=0.0.0.0

# Porta do backend
PORT=8000

# Debug mode (true em dev, false em prod)
DEBUG=true

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# APP INFO
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

APP_NAME=Sistema de Finanças API
APP_VERSION=1.0.0

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NOTAS IMPORTANTES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. Nunca commite o arquivo .env no git!
# 2. Use senhas fortes em produção
# 3. JWT_SECRET_KEY deve ser único por ambiente
# 4. Para PostgreSQL local, primeiro crie o database:
#    createdb finup_db_dev
#    psql finup_db_dev -c "CREATE USER finup_user WITH PASSWORD 'sua_senha';"
#    psql finup_db_dev -c "GRANT ALL PRIVILEGES ON DATABASE finup_db_dev TO finup_user;"

# =============================================================================
# .env.prod.example — Template de variáveis de produção
# =============================================================================
#
# INSTRUÇÕES:
#   1. Copiar este arquivo: cp .env.prod.example .env.prod
#   2. Preencher TODOS os valores marcados com CHANGE_ME
#   3. NUNCA commitar .env.prod no git (já está no .gitignore)
#   4. chmod 600 .env.prod   ← apenas root lê
#
# Gerar secrets:
#   JWT_SECRET_KEY: python3 -c "import secrets; print(secrets.token_hex(32))"
#   POSTGRES_PASSWORD: openssl rand -base64 32
# =============================================================================

# ─── PostgreSQL ───────────────────────────────────────────────────────────────
POSTGRES_USER=finup_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=finup_db

# ─── Backend ──────────────────────────────────────────────────────────────────
# ATENÇÃO: usa 'postgres' (nome do serviço Docker), não 'localhost'
DATABASE_URL=postgresql://finup_user:CHANGE_ME_STRONG_PASSWORD@postgres:5432/finup_db
REDIS_URL=redis://redis:6379/0

JWT_SECRET_KEY=CHANGE_ME_GENERATE_WITH_secrets.token_hex_32

# Origens permitidas pelo CORS (domínios do frontend)
BACKEND_CORS_ORIGINS=https://meufinup.com.br,https://admin.meufinup.com.br

# ─── Frontend (baked no build — deve ser URL pública do backend) ───────────────
# Se Nginx proxy /api → backend: usar o domínio público
NEXT_PUBLIC_BACKEND_URL=https://meufinup.com.br
